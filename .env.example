# Example environment variables for local development and deployment
# Copy this file to .env.local (or provide as secrets in CI/Vercel)

# Supabase (if using Supabase for production/staging)
SUPABASE_URL=https://your-project-url.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Gemini / Google Gen AI API key (only for AI features)
GEMINI_API_KEY=your_gemini_api_key_here

# Vercel (optional — used by CI to trigger deployments)
VERCEL_TOKEN=your_vercel_token
VERCEL_PROJECT_ID=your_vercel_project_id
VERCEL_ORG_ID=your_vercel_org_id

# Other third-party keys
GITHUB_REPO=frcb79/Mecard-1.0
